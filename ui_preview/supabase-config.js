/* ============================================================
   Trust Wallet demo — public Supabase configuration only.

   This browser bundle must never contain a service-role or secret key.
   A public anon/publishable key is expected here for the legacy preview;
   all privileged writes belong behind RLS-protected RPCs or a server.
   ============================================================ */

const SUPABASE_URL = 'https://bgwsoyfsyoecsuemckel.supabase.co';
const SUPABASE_ANON = '';
