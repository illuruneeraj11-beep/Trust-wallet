// This widget is superseded by _TokenRow in trending_screen.dart
// Kept as a stub to avoid breaking any future references.
import 'package:flutter/material.dart';

class TrendingTokenCard extends StatelessWidget {
  final VoidCallback onTap;
  const TrendingTokenCard({super.key, required this.onTap});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
